<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <span class="navbar-brand h2" style="padding-left: 15px">TurboShop</span>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path("home") }}">Magasins</a>
            </li>
            {% if is_granted('ROLE_USER') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('my_orders') }}">Mes commandes</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("admin") }}">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("validation") }}">Validation</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("message") }}">Messages</a>
                </li>
            {% endif %}
        </ul>
    </div>

    <div class="collapse navbar-collapse justify-content-end">
        <ul class="navbar-nav">

            <li class="nav-item">
                <a class="nav-link" href="{{ path("basket") }}">Panier <span class="badge badge-pill badge-light">{{ app.session.get('panier')|length }}</span></a>
            </li>

            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_logout") }}">Déconnecter : {{ app.user.username }}</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_login") }}">Se connecter</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_register") }}">S'inscrire</a>
                </li>
            {% endif %}


        </ul>
    </div>
</nav>